#include<stdio.h>
#include<stdlib.h>
#include<math.h>

int main()

{
    int m[3][3],s1=0,st=0,p=1,i,j;
    float media=0;
   
    for(i=0;i<3;i++)
    {
        for(j=0;j<3;j++)
        {
            printf("Digite o valor da linha %d e coluna %d: ",i,j);
            scanf("%d",&m[i][j]);
        }
    }
   
     
    for(i=0;i<3;i++)
    {
        printf("\n");
        for(j=0;j<3;j++)
        {printf("%d ",m[i][j]);
    if(j<1)
    {
    s1=s1+m[i][j];
    }
    else if(i<1)
    {
    p=p*m[i][j];
    }
        }
    }
     for(i=0;i<3;i++)
    {
        for(j=0;j<3;j++)
        {
    if(i<3)
    {
    st=st+m[i][j];
    }
        }
    }printf("\n");
    printf("A soma e: %d\n",s1);
    printf("O produto e: %d\n",p);
    printf("A soma total e: %d\n",st);
    printf("A media e: %.1f\n",media=(media+st)/9);
}

